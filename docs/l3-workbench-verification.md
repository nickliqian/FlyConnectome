# L3 工作台验证记录

2026-09-17，本机 macOS，网页服务使用项目 conda Python 3.12，物理进程使用已有 FlyGym 2.0.1 / MuJoCo 3.6 环境。

- 14 项 Python 测试通过：配置校验、本机 HTTP 访问边界、重置／导出在途状态保护，以及真实物理集成测试。
- 4 项 JavaScript 测试通过：共享配置同步、本地场景草稿保护、指令确认后同步、预设暂存。
- Python 编译检查与 JavaScript 语法检查通过。
- 实际浏览器验证：启动、暂停、0.1 秒步进、手动前进／归零、罗盘遮挡与恢复、目标拖动／数字编辑、相机切换、导出、两个页面配置同步，以及重置期间刷新。
- 390 px 手机宽度验证无横向溢出，桌面界面已检查。

默认双目标自主导航实验 `20260917-101449-139e75` 完成 10 秒仿真，进入最终目标约 3.3 mm 范围后运动信号归零。航向估计 RMSE 约 4.8°，累计计算约 114.1 秒。结果位于 `outputs/l3-workbench/20260917-101449-139e75/experiment.zip`。这是一组本机验证结果，不代表所有参数组合都能成功导航或保持相同运行速度。

关键回归：MuJoCo 静态合并可能移除未被相机引用的胸部节点，导致 FlyGym 2.0.1 的身体索引为 -1。工作台保留俯视跟随相机，并明确检查胸部索引；测试同时核对正常的初始胸部高度和航向。重置／导出使用指令编号确认，旧反馈不能提前解除操作锁。

独立只读代码审查发现的两项状态同步问题已修复并复核。

建议提交信息：`feat(l3): 新增可交互的本地仿真工作台`
